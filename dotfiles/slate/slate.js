
var hyper = "ctrl;shift;alt;cmd";

function generateActions(gridWidth, gridHeight) {
  // var array = [];
  // for 
  //   var array = options
}

slate.bind("1:" + hyper, function(win) {
  win.doOperation();
});
